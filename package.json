{
  "name": "sito",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm install && echo 'Build completed'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.1"
  }
}